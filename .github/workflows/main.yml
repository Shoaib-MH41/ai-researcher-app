name: Diagnostic Flutter Build

on:
  push:
  pull_request:

jobs:
  diagnostic-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.2'
          channel: 'stable'

      - name: Verify Flutter installation
        run: flutter --version

      - name: Check project structure
        run: |
          echo "=== CHECKING PROJECT STRUCTURE ==="
          for dir in lib android ios; do
            if [ -d "$dir" ]; then
              echo "✅ Folder found: $dir"
            else
              echo "❌ Missing folder: $dir"
            fi
          done
          if [ -f "pubspec.yaml" ]; then
            echo "✅ Found pubspec.yaml"
          else
            echo "❌ Missing pubspec.yaml"
          fi

      - name: Install dependencies
        run: flutter pub get

      - name: Static Code Analysis
        run: |
          echo "=== STATIC CODE ANALYSIS ==="
          flutter analyze --no-fatal-infos --no-fatal-warnings || true

      - name: Summary
        run: |
          echo "✅ Diagnostic completed successfully."
          echo "🚫 Icon or assets not checked (ignored as per setup)."
