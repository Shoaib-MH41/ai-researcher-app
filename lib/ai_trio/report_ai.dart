import 'dart:io';
import 'package:flutter/foundation.dart';
import 'package:path_provider/path_provider.dart';
import 'package:pdf/pdf.dart';
import 'package:pdf/widgets.dart' as pw;

/// 📄 ReportAI —
/// تمام AI ماڈیولز کے نتائج کو لے کر مکمل PDF ریسرچ رپورٹ تیار کرتا ہے۔
class ReportAI {
  static Future<String> generatePDFReport({
    required String problem,
    required Map<String, dynamic> researchData,
    required Map<String, dynamic> labResults,
    required Map<String, dynamic> analysisData,
    required Map<String, dynamic> treatmentPlan,
    required Map<String, dynamic> bioFindings,
  }) async {
    debugPrint('🧾 Generating PDF report for: $problem');

    final pdf = pw.Document();

    pdf.addPage(
      pw.MultiPage(
        margin: const pw.EdgeInsets.all(24),
        build: (context) => [
          pw.Header(
            level: 0,
            child: pw.Text('🔬 Medical Research Report',
                style: pw.TextStyle(fontSize: 22, fontWeight: pw.FontWeight.bold)),
          ),
          pw.Paragraph(text: '🧠 Topic: $problem'),
          pw.SizedBox(height: 16),

          pw.Text('📘 Research Summary', style: pw.TextStyle(fontSize: 18, fontWeight: pw.FontWeight.bold)),
          pw.Paragraph(text: researchData['summary'] ?? 'No research summary available.'),

          pw.SizedBox(height: 12),
          pw.Text('🧪 Lab Testing Results', style: pw.TextStyle(fontSize: 18, fontWeight: pw.FontWeight.bold)),
          pw.Paragraph(text: labResults['results'] ?? 'No lab data available.'),

          pw.SizedBox(height: 12),
          pw.Text('📈 Medical Analysis', style: pw.TextStyle(fontSize: 18, fontWeight: pw.FontWeight.bold)),
          pw.Paragraph(text: analysisData['insights'] ?? 'No analysis available.'),

          pw.SizedBox(height: 12),
          pw.Text('💊 Treatment Plan', style: pw.TextStyle(fontSize: 18, fontWeight: pw.FontWeight.bold)),
          pw.Paragraph(text: treatmentPlan['plan'] ?? 'No treatment plan available.'),

          pw.SizedBox(height: 12),
          pw.Text('🧬 Biological Findings', style: pw.TextStyle(fontSize: 18, fontWeight: pw.FontWeight.bold)),
          pw.Paragraph(text: bioFindings['summary'] ?? 'No biological findings available.'),

          pw.SizedBox(height: 16),
          pw.Divider(),
          pw.Text('📅 Generated by AI Trio System', style: pw.TextStyle(fontSize: 12, color: PdfColors.grey700)),
        ],
      ),
    );

    // فائل محفوظ کرنا
    final outputDir = await getApplicationDocumentsDirectory();
    final filePath = '${outputDir.path}/research_report_${DateTime.now().millisecondsSinceEpoch}.pdf';
    final file = File(filePath);
    await file.writeAsBytes(await pdf.save());

    debugPrint('✅ PDF generated successfully: $filePath');
    return filePath;
  }
}
