name: medical_research_app
description: A medical research app with AI capabilities
publish_to: 'none'

version: 1.1.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🧭 UI & Icons
  cupertino_icons: ^1.0.8
  flutter_material_color_picker: ^1.2.0  # (optional: advanced UI color options)

  # 🔐 Secure storage (for saved data / tokens)
  flutter_secure_storage: ^9.2.2

  # 📄 PDF generation & printing
  pdf: ^3.11.2
  printing: ^5.13.0
  path_provider: ^2.1.4

  # 📤 File Sharing & Opening
  share_plus: ^10.0.2     # ✅ Share PDF files
  open_filex: ^4.5.0      # ✅ Open PDF in default viewer

  # 🌐 Network & AI integration
  http: ^1.2.2
  provider: ^6.1.2

  # 🧠 (Optional future AI APIs)
  google_generative_ai: ^0.3.0     # Gemini integration (optional)
  tflite_flutter: ^0.10.4          # For on-device AI models (optional)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  # ✅ Fonts (for Urdu/Arabic PDF support)
  assets:
    - assets/fonts/
  fonts:
    - family: NotoNastaliqUrdu
      fonts:
        - asset: assets/fonts/NotoNastaliqUrdu-Regular.ttf
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
