name: medical_research_app
description: A medical research app with AI capabilities
publish_to: 'none'

version: 1.1.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🧭 UI & Icons
  cupertino_icons: ^1.0.8
  flutter_material_color_picker: ^1.2.0

  # 🔐 Secure storage
  flutter_secure_storage: ^9.2.2

  # 📄 PDF generation & printing
  pdf: ^3.11.2
  printing: ^5.13.0
  path_provider: ^2.1.4

  # 📤 File Sharing & Opening
  share_plus: ^10.0.2
  open_filex: ^4.5.0

  # 🌐 Network & AI integration
  http: ^1.2.2
  provider: ^6.1.2

  # 🧠 (Optional future AI APIs)
  google_generative_ai: ^0.3.0

  # 🗓️ Date & Localization
  intl: ^0.17.0     # ✅ یہ لازمی add کرنا ہے

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  # ✅ Fonts & Assets for PDF
  assets:
    - assets/fonts/NotoNastaliqUrdu-VariableFont_wght.ttf
    - assets/fonts/Amiri-Regular.ttf
    - assets/fonts/OpenSans-VariableFont_wdth,wght.ttf

  fonts:
    - family: NotoNastaliqUrdu
      fonts:
        - asset: assets/fonts/NotoNastaliqUrdu-VariableFont_wght.ttf
    - family: Amiri
      fonts:
        - asset: assets/fonts/Amiri-Regular.ttf
    - family: OpenSans
      fonts:
        - asset: assets/fonts/OpenSans-VariableFont_wdth,wght.ttf
